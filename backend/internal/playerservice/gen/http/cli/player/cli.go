// Code generated by goa v3.5.5, DO NOT EDIT.
//
// player HTTP client CLI support package
//
// Command:
// $ goa gen github.com/JordanRad/play-j/backend/internal/design/player-service
// -o ./internal/playerservice

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	playerc "github.com/JordanRad/play-j/backend/internal/playerservice/gen/http/player/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `player get-track-by-id
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` player get-track-by-id --track-id 16082704779295728655` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		playerFlags = flag.NewFlagSet("player", flag.ContinueOnError)

		playerGetTrackByIDFlags       = flag.NewFlagSet("get-track-by-id", flag.ExitOnError)
		playerGetTrackByIDTrackIDFlag = playerGetTrackByIDFlags.String("track-id", "REQUIRED", "ID of the Track")
	)
	playerFlags.Usage = playerUsage
	playerGetTrackByIDFlags.Usage = playerGetTrackByIDUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "player":
			svcf = playerFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "player":
			switch epn {
			case "get-track-by-id":
				epf = playerGetTrackByIDFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "player":
			c := playerc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get-track-by-id":
				endpoint = c.GetTrackByID()
				data, err = playerc.BuildGetTrackByIDPayload(*playerGetTrackByIDTrackIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// playerUsage displays the usage of the player command and its subcommands.
func playerUsage() {
	fmt.Fprintf(os.Stderr, `Player operations
Usage:
    %[1]s [globalflags] player COMMAND [flags]

COMMAND:
    get-track-by-id: GetTrackByID implements getTrackByID.

Additional help:
    %[1]s player COMMAND --help
`, os.Args[0])
}
func playerGetTrackByIDUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] player get-track-by-id -track-id UINT

GetTrackByID implements getTrackByID.
    -track-id UINT: ID of the Track

Example:
    %[1]s player get-track-by-id --track-id 16082704779295728655
`, os.Args[0])
}
